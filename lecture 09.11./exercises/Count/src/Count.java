public class Count {

    public static void main(String[] args) {
        String text = "This text contains various characters. How many times does the character 'e' appear?";
        char c = 'e';

        int count = countCharacters(text, c);

        System.out.println("The character '" + c + "' appeared " + count + " times.");
    }

    public static int countCharacters(String text, char c) {
        // Now it's your turn!
        // Implement a function that counts how many times
        // the character given by the parameter c appears in
        // the string text, and returns that value.
    }

}
