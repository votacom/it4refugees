package connectfour;

/**
 * Enumeration representing the color of a tile. Can be either Red or Blue.
 */
public enum TileColor {
    Red("X"), Blue("O");

    TileColor(String str) {
        // TODO: store the string representation in a property so it can be returned by toString()
    }

    @Override
    public String toString() {
        // TODO: return the correct string representation here
        return "";
    }
}
