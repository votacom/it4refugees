/**
 * Write a guessing game where the user has to guess a secret number.
 * After every guess the program tells the user whether their number was too large
 * or too small. At the end the number of tries needed should be printed.
 * Try to use functions where it makes sense, for example, a function that takes
 * the number the user guessed as a parameter and returns either true or false
 * depending on whether or not the guess was correct.
 */
public class Guess {

    public static void main(String[] args) {

    }
}
