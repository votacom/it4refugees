/**
 * Write a function that takes a String as input parameter,
 * and returns an array with the characters of that string
 * as its elements.
 */
public class StringToArray {

    public static void main(String[] args) {
        String input = "characters";
        char[] characters = getCharactersFromString(input); // should return {'c', 'h', 'a', 'r', 'a', 'c', 't', 'e', 'r', 's'}
    }

    public static char[] getCharactersFromString(String input) {
        return null;
    }
}
